# v0.2.0 — Bugfixes, Scoring Tiers, Invite Sharing, Results UI, Leaderboard, What If?

**Overall Progress:** `~9%` (1/11 steps done)

## Context
v0.1.2 shipped with several UX gaps and a settings bug. This release fixes the disabled-select form bug, makes results entry discoverable, adds social sharing for invites, implements tiered scoring, enhances the pre-results leaderboard, and adds a "What If?" simulation for admins.

## Critical Decisions
- **Disabled select fix**: Hidden input preserves value — standard HTML fix
- **Results link**: Pool detail action bar, visible to ADMIN + RESULTS_MANAGER
- **Invite sharing**: Compose `InviteShareButtons` around existing `CopyInviteLink` — don't duplicate clipboard logic
- **Scoring tiers**: 4-tier system (180/90/30/15), 0.6x runner-up multiplier. Seed must explicitly write `runnerUpMultiplier: 0.6` in upsert update blocks (schema default only applies to new rows)
- **Pre-results leaderboard**: "Show only my stats" privacy model — current user sees own completion %, others show sealed badges. No podium/envelope complexity — keep scope minimal.
- **What If?**: Client-side only using `calculateLeaderboard()` (pure function). Uses shadcn Sheet component. Swaps leaderboard entries in-place with "SIMULATION" banner. Admin-only.
- **`?created=1` guard**: Admin-only check + `router.replace()` URL cleanup to prevent dialog re-showing on refresh
- **Prisma queries**: Extract raw `prisma.category.findMany()` from leaderboard page into `src/lib/db/categories.ts`
- **Semantic tokens**: Fix `bg-gray-300/15`, `text-gray-300` violations in LeaderboardTable while we're touching it

## GitHub Issues
| Step | Issue | Title |
|------|-------|-------|
| 1 | #48 | fix: disabled accessType select |
| 2 | #49 | feat: Enter Results link |
| 3 | #50 | feat: invite sharing dialog + inline |
| 4 | #51 | feat: tiered scoring + 0.6x runner-up |
| 5 | #52 | feat: pre-results leaderboard (includes query extraction + token fixes) |
| 6 | #52 | feat: pre-results leaderboard (UI component) |
| 7 | #53 | feat: What If? simulator |

## Parallel Execution Groups
```
Group A (independent — can run in parallel):
  ├─ Step 1: Fix disabled select (#48)        — 1 file, no deps
  ├─ Step 2: Enter Results link (#49)          — 1 file, no deps on other steps
  └─ Step 4: Tiered scoring (#51)             — seed + demo data + schema + tests, no deps

Group B (after Group A completes):
  ├─ Step 3: Invite sharing (#50)             — touches pool detail page (same as Step 2)
  └─ Step 5: Extract queries + tokens (#52)   — foundation for Steps 6+7

Group C (after Step 5):
  ├─ Step 6: Pre-results leaderboard (#52)    — uses extracted DB helpers
  └─ Step 7: What If? simulator (#53)         — uses getCategoriesWithNominees

Group D (sequential, after all above):
  └─ Steps 8-11: Tests, issues, validate, deploy
```

## Tasks

- [ ] 🟥 **Step 1: Fix disabled accessType select not submitting value (#48)**
  - [ ] 🟥 In `src/app/pools/[id]/settings/pool-settings-form.tsx`: add `<input type="hidden" name="accessType" value={accessType} />` when `isAlreadyOpen` is true
  - **Root cause**: Disabled form controls excluded from FormData → Zod rejects missing `accessType`
  - **File**: `src/app/pools/[id]/settings/pool-settings-form.tsx`

- [ ] 🟥 **Step 2: Add "Enter Results" link to pool detail page (#49)**
  - [ ] 🟥 Import `checkResultsPermission` from `@/lib/results/permissions`
  - [ ] 🟥 Call it, add "Enter Results" button (icon: `ClipboardCheck`, variant: `outline`) in action nav bar, visible when `canSetResults === true`
  - [ ] 🟥 Link: `/results/${pool.ceremonyYear.id}`
  - **File**: `src/app/pools/[id]/page.tsx`

- [ ] 🟥 **Step 3: Invite sharing — dialog + inline upgrade (#50)**
  - [ ] 🟥 Add shadcn Dialog: `npx shadcn@latest add dialog`
  - [ ] 🟥 Create `src/components/pools/InviteShareButtons.tsx` — reusable component that:
    - Composes around existing `CopyInviteLink` for clipboard functionality (don't duplicate)
    - Adds WhatsApp button (`https://wa.me/?text=...`)
    - Adds X/Twitter button (`https://twitter.com/intent/tweet?text=...`)
  - [ ] 🟥 Create `src/components/pools/InviteShareDialog.tsx` — wraps Dialog + InviteShareButtons, auto-opens via `open` prop
  - [ ] 🟥 Update `src/app/pools/create/actions.ts`: redirect to `/pools/${poolId}?created=1`
  - [ ] 🟥 Update `src/app/pools/[id]/page.tsx`:
    - `searchParams` is a Promise in Next.js 15 — await it
    - Detect `?created=1` + verify user is ADMIN before showing dialog
    - Server passes `showCreatedDialog={created === "1" && isAdmin}` prop
    - `InviteShareDialog` (client component) calls `router.replace()` to clean URL on mount
    - Render `<InviteShareDialog>` as client component child of server page
  - [ ] 🟥 Replace `<CopyInviteLink>` in pool detail invite card with `<InviteShareButtons>` — shows WhatsApp/X/Copy inline
  - **Files**: `InviteShareButtons.tsx` (new), `InviteShareDialog.tsx` (new), `create/actions.ts`, `pools/[id]/page.tsx`, `CopyInviteLink.tsx` (kept, composed into)

- [ ] 🟥 **Step 4: Update scoring to tiered point values (#51)**
  - [ ] 🟥 Update `prisma/seed.ts`:
    - Add `runnerUpMultiplier` to `CategoryInput` type
    - Set `runnerUpMultiplier: 0.6` on ALL category entries (both ceremony2025 and ceremony2026)
    - Include `runnerUpMultiplier` in upsert update block (not just create)
    - Apply tier point values:

      | Tier | Points | Categories |
      |------|--------|------------|
      | 1 | 180 | Best Picture, Director, Actor, Actress, Supporting Actor, Supporting Actress |
      | 2 | 90 | Film Editing, Cinematography, VFX, Original Screenplay, Adapted Screenplay, Animated Feature |
      | 3 | 30 | Costume Design, Production Design, Makeup & Hairstyling, Original Song, Sound |
      | 4 | 15 | Animated Short, Live Action Short, Documentary Short, Documentary Feature, International Feature, Original Score, Best Casting (2026) |

  - [ ] 🟥 Update `src/lib/demo/oscar-data.ts` — same tier values
  - [ ] 🟥 Update `prisma/schema.prisma` — change `runnerUpMultiplier` default from `0.5` to `0.6`
  - [ ] 🟥 Update `__tests__/lib/scoring/calculate-score.test.ts` — adjust tests using default multiplier
  - [ ] 🟥 Update `docs/SCHEMA.md` — document tier system and 0.6x rationale

- [ ] 🟥 **Step 5: Extract Prisma queries + fix semantic tokens (#52)**
  - [ ] 🟥 Create `src/lib/db/categories.ts` with `getCategoriesByCeremonyYear(ceremonyYearId)` — extract raw `prisma.category.findMany()` from leaderboard page (lines 62-76)
  - [ ] 🟥 Add `getCategoriesWithNominees(ceremonyYearId)` to same file — includes nominees (needed by What If?)
  - [ ] 🟥 Fix semantic token violations in `src/components/leaderboard/LeaderboardTable.tsx`:
    - `bg-gray-300/15` → `bg-secondary/50`
    - `text-gray-300` → `text-muted-foreground`
  - **Files**: `src/lib/db/categories.ts` (new), `LeaderboardTable.tsx`, leaderboard `page.tsx`

- [ ] 🟥 **Step 6: Pre-results leaderboard — minimal scope (#52)**
  - [ ] 🟥 Replace current "Predictions Not Yet Locked" state (Lock icon + text) in leaderboard page
  - [ ] 🟥 Create `src/components/leaderboard/PreResultsLeaderboard.tsx`:
    - **Privacy model**: "Show only my stats"
    - Current user's card: name, avatar, "X of Y categories predicted" completion count
    - Other members: sealed badge only (no prediction stats visible)
    - Status banner: "Predictions locked — awaiting ceremony results"
    - Member count: "X members competing"
    - Use `animate-shimmer` or `animate-glow-pulse` for subtle gold accent
  - [ ] 🟥 Server passes: current user prediction count, total categories, member count, current user info
  - **Files**: `src/app/pools/[id]/leaderboard/page.tsx`, `src/components/leaderboard/PreResultsLeaderboard.tsx` (new)

- [ ] 🟥 **Step 7: "What If?" simulation — admin-only (#53)**
  - [ ] 🟥 Add shadcn Sheet: `npx shadcn@latest add sheet`
  - [ ] 🟥 Create `src/components/leaderboard/WhatIfSimulator.tsx` — client component:
    - "What If?" button on leaderboard page (visible to ADMIN only)
    - Opens Sheet (slide-in panel) with two modes:
      - **Random**: "Randomize All" button picks random nominee per category → recalculates leaderboard
      - **Manual**: Per-category dropdown to select hypothetical winner
    - Reuses `calculateLeaderboard()` (pure function, works client-side)
    - Swaps leaderboard entries in-place with "SIMULATION" banner (not a separate table)
    - "Reset" button clears mock winners, restores real leaderboard
    - **No DB writes** — purely client-side computation
  - [ ] 🟥 Update leaderboard `page.tsx`:
    - Use `getCategoriesWithNominees()` from Step 5 (includes nominees for dropdowns)
    - Pass `isAdmin` prop based on `memberRole === "ADMIN"`
    - Pass categories with nominees + member predictions as serializable props
  - **Files**: `src/components/leaderboard/WhatIfSimulator.tsx` (new), `src/app/pools/[id]/leaderboard/page.tsx`

- [ ] 🟥 **Step 8: Tests for new components**
  - [ ] 🟥 Test `InviteShareButtons` — renders all share buttons, clipboard works
  - [ ] 🟥 Test `PreResultsLeaderboard` — shows own stats, hides others
  - [ ] 🟥 Test `WhatIfSimulator` — random mode recalculates, manual mode works, reset clears
  - [ ] 🟥 Update scoring tests for 0.6x multiplier

- [x] 🟩 **Step 9: Create GitHub issues**
  - [x] 🟩 #48: disabled select bug
  - [x] 🟩 #49: results UI discoverability
  - [x] 🟩 #50: invite sharing (dialog + inline)
  - [x] 🟩 #51: scoring tier system
  - [x] 🟩 #52: pre-results leaderboard design
  - [x] 🟩 #53: "What If?" simulator

- [ ] 🟥 **Step 10: Validate**
  - [ ] 🟥 `npx tsc --noEmit` — no type errors
  - [ ] 🟥 `npx eslint .` — 0 errors
  - [ ] 🟥 `npx vitest run` — all tests pass
  - [ ] 🟥 `npm run build` — production build succeeds

- [ ] 🟥 **Step 11: Update Vercel production data (last)**
  - [ ] 🟥 Run `npm run seed` against production DATABASE_URL
  - [ ] 🟥 Seed upserts update both `pointValue` AND `runnerUpMultiplier: 0.6` (from Step 4 changes)
  - **Note**: Must be done AFTER all code changes validated and deployed

## Scoring Reference

```
Tier 1 — 180pts / 108pts runner-up (0.6x)
  Best Picture, Director, Actor, Actress, Supporting Actor, Supporting Actress

Tier 2 — 90pts / 54pts runner-up
  Editing, Cinematography, VFX, Original Screenplay, Adapted Screenplay, Animated Feature

Tier 3 — 30pts / 18pts runner-up
  Costume Design, Production Design, Makeup, Song, Sound

Tier 4 — 15pts / 9pts runner-up
  Shorts (Live/Animated/Doc), Documentary Feature, International Feature, Score, Casting
```

## What If? Architecture

```
Server (leaderboard page.tsx)
  ├─ getCategoriesWithNominees(ceremonyYearId)  ← new DB helper
  ├─ Fetch predictions (existing)
  ├─ Calculate real leaderboard (existing)
  ├─ Pass { categories, nominees, predictions, isAdmin } to client
  │
Client (WhatIfSimulator.tsx)
  ├─ Admin clicks "What If?" → Sheet opens
  ├─ Random: pick random nominee per category → mockWinnerIds map
  ├─ Manual: admin selects via dropdowns → mockWinnerIds map
  ├─ Build ScoringInput[] with mock winnerIds
  ├─ Call calculateLeaderboard() (pure function)
  └─ Swap leaderboard entries in-place + "SIMULATION" banner
```

No DB writes. No API calls. Pure client-side computation.

## Verification
1. `npx tsc --noEmit` — no type errors
2. `npx eslint .` — 0 errors
3. `npx vitest run` — all tests pass
4. `npm run build` — production build succeeds
5. Pool settings Open pool → Save → no error
6. Pool detail → "Enter Results" visible for admin/results manager
7. Create pool → dialog with WhatsApp/X/Copy buttons
8. Pool detail invite card → shows WhatsApp/X/Copy (not just copy)
9. Demo leaderboard → updated tier point values
10. Leaderboard pre-results → shows own stats, sealed badges for others
11. Leaderboard as admin → "What If?" button → randomize → simulated leaderboard shown
12. `npm run seed` → production data updated with tier values + 0.6x multiplier
